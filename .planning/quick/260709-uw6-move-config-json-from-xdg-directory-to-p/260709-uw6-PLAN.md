---
quick_id: 260709-uw6
type: execute
wave: 1
files_modified:
  - internal/config/config.go
  - internal/config/config_test.go
autonomous: true
must_haves:
  truths:
    - ConfigDir returns current working directory, not XDG path
    - ConfigPath returns ./config.json in CWD
    - Tests pass with new CWD-based behavior
  artifacts:
    - internal/config/config.go (modified)
    - internal/config/config_test.go (modified)
  key_links:
    - main.go uses config.ConfigPath() and config.Load() seamlessly
---

<objective>
Move the config JSON file from XDG_CONFIG_HOME-based path to the project root (current working directory). Config file becomes ./config.json instead of ~/.config/animeheaven/config.json.
</objective>

<tasks>

<task type="auto">
  <name>Update ConfigDir to return CWD</name>
  <files>internal/config/config.go</files>
  <action>
    Replace ConfigDir() to return os.Getwd() instead of the XDG_CONFIG_HOME-based path. Remove XDG_CONFIG_HOME env var usage and os.UserConfigDir() fallback. Update the Config struct comment to reflect ./config.json.
  </action>
  <verify>go build ./internal/config/...</verify>
  <done>ConfigDir returns CWD, ConfigPath returns CWD/config.json</done>
</task>

<task type="auto">
  <name>Update config tests</name>
  <files>internal/config/config_test.go</files>
  <action>
    Replace TestConfigDirUsesXdgConfigHome and TestConfigDirFallsBackToUserConfigDir with TestConfigDirReturnsCwd that verifies ConfigDir matches os.Getwd(). Update TestConfigPath to verify path is CWD/config.json.
  </action>
  <verify>go test ./internal/config/... -v</verify>
  <done>All config tests pass with new CWD-based behavior</done>
</task>

</tasks>

<success_criteria>
- go build passes
- go vet passes
- All tests pass
- Config file is now at ./config.json (CWD)
</success_criteria>
